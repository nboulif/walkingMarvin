from setuptools import setup

# setup(name='gym_foo',
#       version='0.0.1',
#       install_requires=['gym']  # And any other dependencies foo needs
# )  
setup(name='gym_marvin',
    version='0.0.1',
    install_requires=['gym']  # And any other dependencies foo needs
)  